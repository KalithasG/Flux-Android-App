<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release builds: system CAs only (Android default). -->
    <base-config>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Debug builds only: also trust user-added CAs so development works
         behind TLS-inspecting software (e.g. antivirus/corporate proxies). -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
